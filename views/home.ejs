<title><%= title %></title>
<div class="wrapper-login">
  <% if (!user) { %>
    <p>Welcome! Please <a href="/auth/github">log in</a>.</p>
  <% } else { %>
    <p>Hello, <%= user.username %>. View your <a href="/profile">profile</a>.</p>
  <% } %>
</div>
